node_modules/
web/vendor/
.env
.DS_Store

# Your own character / motion assets. Keep the folders, ignore the binaries.
models/*
motions/*
!models/.gitkeep
!motions/.gitkeep
# ...but the descriptions are metadata, not an asset — keep them versioned.
!motions/motions.json
